resources:
  dashboards:
    bakehouse_dashboard:
      # No [${bundle.target}] prefix here: dev mode already prepends "[dev <user>]".
      display_name: "Bakehouse — Vendas & Sentimento"
      file_path: ../src/dashboards/bakehouse_dashboard.lvdash.json
      warehouse_id: 03a88eadb972f5b4
      # Datasets use unqualified table names; these resolve them to the bundle catalog/schema.
      dataset_catalog: ${var.catalog}
      dataset_schema: ${var.schema}
